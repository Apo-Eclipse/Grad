DATABASE SCHEMA (based on Django models):

TABLE: core_transaction
  - id (bigint, PK)
  - user_id (bigint, FK -> auth_user.id, nullable)
  - budget_id (bigint, FK -> core_budget.id, nullable)
  - account_id (bigint, FK -> core_account.id, nullable)
  - transfer_to_id (bigint, FK -> core_account.id, nullable)
  - date (date)
  - amount (numeric(12,2))
  - time (time, nullable)
  - description (text, nullable)  # Replaces store_name
  - category (text, nullable)     # Replaces type_spending
  - city (text, nullable)
  - neighbourhood (text, nullable)
  - active (boolean, default true)
  - created_at (timestamp with time zone, auto_now_add)
  - updated_at (timestamp with time zone, nullable)

TABLE: core_budget
  - id (bigint, PK)
  - user_id (bigint, FK -> auth_user.id)
  - budget_name (text)
  - description (text, nullable)
  - total_limit (numeric(12,2))
  - priority_level_int (smallint, nullable)
  - active (boolean, default true)
  - icon (text, default 'cash-outline')
  - color (text, default '#10b981')
  - created_at (timestamp with time zone, auto_now_add)
  - updated_at (timestamp with time zone, nullable)

TABLE: core_income
  - id (bigint, PK)
  - user_id (bigint, FK -> auth_user.id)
  - type_income (text)
  - account_id (bigint, FK -> core_account.id, nullable)
  - amount (numeric(12,2), nullable)
  - description (text, nullable)
  - active (boolean, default true)
  - created_at (timestamp with time zone, auto_now_add)
  - updated_at (timestamp with time zone, nullable)

TABLE: core_goal
  - id (bigint, PK)
  - user_id (bigint, FK -> auth_user.id)
  - goal_name (text)
  - description (text, nullable)
  - target (numeric(12,2), nullable)
  - start_date (date, auto_now_add)
  - due_date (date, nullable)
  - plan (text, nullable)
  - saved_amount (numeric(12,2), default 0.00)
  - icon (text, default 'flag-outline')
  - color (text, default '#10b981')
  - active (boolean, default true)
  - created_at (timestamp with time zone, auto_now_add)
  - updated_at (timestamp with time zone, nullable)

TABLE: core_notification
  - id (bigint, PK)
  - user_id (bigint, FK -> auth_user.id)
  - title (varchar(255))
  - message (text)
  - is_read (boolean, default false)
  - notification_type (varchar(50), default 'info')
  - created_at (timestamp with time zone, auto_now_add)

TABLE: core_account
  - id (bigint, PK)
  - user_id (bigint, FK -> auth_user.id)
  - name (text)
  - balance (numeric(12,2), default 0.00)
  - type (text, choices: REGULAR, SAVINGS, CREDIT)
  - active (boolean, default true)
  - created_at (timestamp with time zone, auto_now_add)
  - updated_at (timestamp with time zone, nullable)

TABLE: core_profile (One-to-One with auth_user)
  - id (bigint, PK)
  - user_id (bigint, FK, Unique -> auth_user.id)
  - job_title (text, nullable)
  - address (text, nullable)
  - birthday (date, nullable)
  - gender (text, nullable, choices)
  - employment_status (text, nullable, choices)
  - education_level (text, nullable, choices)
  - user_persona (jsonb, nullable)
  - created_at (timestamp with time zone, auto_now_add)
  - updated_at (timestamp with time zone, nullable)

TABLE: core_chatconversation
  - id (bigint, PK)
  - user_id (bigint, FK -> auth_user.id)
  - title (text, nullable)
  - channel (text, nullable)
  - summary_text (text, nullable)
  - started_at (timestamp with time zone, auto_now_add)
  - last_message_at (timestamp with time zone, nullable)
  - summary_created_at (timestamp with time zone, nullable)

TABLE: core_chatmessage
  - id (bigint, PK)
  - conversation_id (bigint, FK -> core_chatconversation.id)
  - sender_type (text)
  - source_agent (text)
  - content (text)
  - content_type (text, nullable)
  - language (text, nullable)
  - created_at (timestamp with time zone, auto_now_add)

TABLE: auth_user (Standard Django User)
  - id (bigint, PK)
  - username (text)
  - email (text)
  - first_name (text)
  - last_name (text)
  - ... (other standard auth fields)